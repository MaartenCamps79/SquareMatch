// Deze file wordt gebruikt voor extra functionaliteit zoals voortgang, knoppen die dynamisch werken, etc.
// Als je een back-end wilt toevoegen (zoals NoSQL), kun je deze code uitbreiden voor servercommunicatie.

document.addEventListener('DOMContentLoaded', function () {
    // Hier kunnen we extra functionaliteit toevoegen als we willen.
});
